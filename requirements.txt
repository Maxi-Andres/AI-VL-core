requests>=2.31

# YOLO path only (vlm_common / the VLM path does NOT need this).
# Pulls in torch + opencv; install it when you want to run the YOLO scripts/menu.
ultralytics>=8.3

# HTTP inference service (service.py) — exposes detection over a port so the
# backend gateway can reach it. Not needed for the CLI (menu.py), only the service.
fastapi>=0.110
uvicorn[standard]>=0.29
pillow>=10.0

# Speech-to-text path only (asr_common / the /transcribe endpoint). Runs Whisper
# locally via CTranslate2; pulls in PyAV (bundles ffmpeg) so it decodes the browser's
# webm/opus and mp4/aac clips without a separate ffmpeg binary. The VLM/YOLO paths
# do NOT need this. Model weights (~150 MB for `base`) download on first use.
faster-whisper>=1.0

# Text-to-speech path only (tts_common / the /speak endpoint). Local neural voices
# via Piper (ONNX); nicer + consistent across devices, fully offline. Voice model
# files are downloaded by the installer into piper_voices/. Browser TTS works
# without this. Needs >=1.3 on modern Python (3.12+): 1.2.x pins piper-phonemize,
# which has no wheels there.
piper-tts>=1.3

# Second (nicer) TTS engine: Kokoro (82M-param ONNX, CPU, offline). Runs on the
# same onnxruntime as YOLO; phonemization ships in-process via espeakng-loader, so
# no system espeak-ng is required. The installer downloads the model files into
# kokoro_models/. Optional — Piper and browser voices work without it.
kokoro-onnx>=0.4
